# Example environment file for local overrides
# Copy to .env.local and modify as needed

# Override API base URL for local development
# VITE_API_BASE_URL=http://localhost:8000

# Override app name
# VITE_APP_NAME=bin2nlp UI (Local)

# Override log level (debug, info, warn, error)
# VITE_LOG_LEVEL=debug

# Override polling interval in milliseconds
# VITE_POLLING_INTERVAL=1000